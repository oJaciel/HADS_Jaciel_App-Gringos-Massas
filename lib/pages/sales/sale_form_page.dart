import 'package:flutter/material.dart';

class SaleFormPage extends StatelessWidget {
  const SaleFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Formulário de Venda')));
  }
}
